Every figure below is computed at build time from the raw audit data — the
measurement CSVs are published alongside the pages, so any number here can be
checked against its source. Red means a mobile performance score below 50 out
of 100; green means 90 or better.
